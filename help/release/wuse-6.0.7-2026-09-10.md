# 物色 6.0.7：商品操作栏原样式校准

商品操作栏继续作为详情页可添加、隐藏、删除的公共可配置组件。默认使用原来的白色背景、首页/收藏/购物车三个快捷入口，以及“加入购物车”“立即购买”两个独立胶囊按钮；保留入口、按钮显隐、图标、颜色及通用样式配置。

本次将无主题时的默认入口统一为原来的三个入口，修正客户端按钮间距为原预览的 10px、普通商品按钮高度为 36px（以 375px 画布计）。显式保存的自定义入口和隐藏按钮配置继续生效，不会被默认值覆盖。商品不可用、售罄与预售处理保留。

上一轮的浅蓝底、隐藏购买按钮截图来自专门的自定义测试，不代表默认样式。本次将默认样式和自定义隐藏状态分别验证、分别留图。

## 文件

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `dist/物色-6.0.7-universal.apk` | 32,274,881 | `42DAAC853CB71B0EA39158CA917ABCD6767C643B40FD006D4D2F4FCF86D5B8E2` |
| `dist/hengshu-mall-update.tar.gz` | 86,495,902 | `56B8914581CBE194648EBD9DD55B2D598EB9F203DBFAEAA4A1817F86AEE36426` |

APK 原件为 `template/uni-app/unpackage/release/apk/__UNI__159D54B__20260910151935.apk`。包名 `com.hengshucredit.mall`，版本 6.0.7 / 607。沿用已有自有测试证书，APK v1/v2 签名通过。

## 验证

- 后台生产页面验证默认三个入口、两个 36px 高的胶囊按钮与 10px 间距，组件增删、配置保存重载通过。
- H5 生产构建通过；实际商品页默认按钮宽度、同排位置、间距、圆角、白色背景通过。收藏/分享、显隐、公共样式、可选导航叠放与移除、组件删除重载通过。
- 商品不可用保护、默认回退、有效商品购买入口和自定义入口行为回归通过。
- Android 11 / API 30 模拟器由 6.0.6 覆盖安装至 6.0.7 成功，首次安装时间保持不变。首页、分类、商品详情打开正常，详情操作栏贴底。使用现有线上配置，未修改线上装修或提交商品、购物车、订单数据。
- 更新包 12,406 个文件校验、解压保留已有配置与数据、Compose、Nginx H5 路由、京东 API/Chromium/noVNC 和容器重建后登录数据持久化验证通过。
- `git diff --check` 通过。

默认外观截图：`.build/page-actions/screenshots/h5-detail-default.png` 与 `h5-detail-default-bar.png`；后台截图 `admin-detail-default-bar.png`。自定义隐藏截图单独存为 `h5-detail-custom-hidden-actions.png`。

构建及回归日志在 `.build/page-actions/`，Android 构建日志 `help/dev/.state/wuse-6.0.7-build.log`，签名与截图 `help/dev/.state/wuse-6.0.7-verify/`。

更新包已生成，未上传服务器；APK 未上架应用市场。请按 [配置与部署说明](page-actions-2026-09-10.md) 更新服务器，并覆盖安装 6.0.7。本次没有构建 iOS。
