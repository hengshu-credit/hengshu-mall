# 物色 6.0.15 发布记录

后端更新包已上传至 `root@111.228.52.212:/root/hengshu-mall/crmeb-mall` 并完成解包和服务重启。Android APK 为 6.0.15 / 615，包名 `com.hengshucredit.mall`，沿用原签名，可覆盖 6.0.14 安装。

本次交付包含上一轮权限、充值与会员幂等、退款恢复、任务重试、库存锁、商品经营审核、财务异常监测、装修一致性和优惠说明修复。发布时对应用版本进行递增；本轮没有新增业务行为改动。

| 产物 | 大小（字节） | SHA-256 |
| --- | ---: | --- |
| hengshu-mall-update-6.0.15.tar.gz | 98485269 | `03e79688e1bd03854cdcfb9fb5014b9cbb0ce94231b3b5c9730464ffc0d46ab1` |
| 物色-6.0.15-universal.apk | 32349599 | `4612bc40fbcab6cb97ce28b7ab671eaeecba108dbff42ce21c4b4cee46a37041` |

文件位于项目 `dist/`，各有 `.sha256` 校验文件；`dist/release-6.0.15.json` 记录版本、源码、构建、验证和部署对应关系。后端包内 `release.json` 的原生资源摘要与交付 APK 中的 `app-service.js` 完全相同。

源码提交：`9b357b852e14d42ae0653a8da7cd745d9c0bf352`；dirty：`true`；源码摘要：`7a57640a50018ef0fcc1b2744dd149dede551ff955e029918f32cc82cd841464`，共 3170 个文件。工作区包含本次版本修改以及既有的 `docs/jd-product-collection.md` 修改；未提交或清理用户改动。配置合同为 `decoration-context-2026-09-14`，迁移版本记录在后端包的 `release.json`。

验证结果：方法 39 组、隔离 MySQL 9 组、真实 HTTP 1 组、浏览器 3 组、Android 原生 1 组，共 53 组通过，无必需项跳过。后台、H5、App 三项生产构建均通过。更新包 12535 个文件的 SHA-256 校验通过，已排除环境配置、安装锁、数据库、上传文件和签名私钥等受保护状态。

APK v1/v2 签名及 zipalign 通过；218 个编译资源逐文件一致，48 个原生模块与 6.0.14 一致。支持 arm64-v8a、armeabi-v7a、x86。证书 SHA-256：`7a2c34ea73302cfa070ac47003d3985ddb5bbe886431885c4a3776d8f10be7d8`。Android 11 / WebView 83 模拟器完成 6.0.14 → 6.0.15 覆盖升级，首次安装时间和测试数据标记保留。首页、商城分类、购物车、详情、店铺、店铺分类及跨页预览均通过，六页截图已人工核对。

原生回归前两轮因导航未响应失败；诊断发现模拟器临时资源出现空文件。恢复经过校验的 APK 资源并同步落盘后，同一套原生回归完整通过。发布 APK 未混入临时测试钩子。对应临时验证脚本及记录在 `.build/release-6.0.15/`，页面截图在 `.build/commerce-hardening-20260914/`。

远程执行严格限于在指定目录覆盖后端更新包、停止相关服务、解包、构建并启动相关服务。上传和重启命令退出码均为 0，完成时间 `2026-09-14T09:47:21.359197+00:00`；具体输出保留在 `.build/release-6.0.15/restart.log`。未执行独立远程巡检、清理或数据库命令。

验证边界：Android 和浏览器页面使用本地隔离 API 与已保存公开配置回放；真实 HTTP 与交易数据库回归也在本地隔离环境运行。本次没有额外探测线上接口或调用真实支付渠道，不能把本地回放解释为线上交易验收。APP 交付为签名 Android 安装包，未提交应用商店，未发布 iOS 或小程序。临时 SSH 私钥沿用上一轮状态，本轮未尝试清理。
