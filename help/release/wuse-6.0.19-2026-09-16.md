# 物色 6.0.19 发布记录（2026-09-16）

已更新生产环境 PHP 后端、管理后台和 H5；安卓安装包沿用原包名与签名，下载地址：https://mall.hengshucredit.com/downloads/wuse-6.0.19.apk。APK 为 6.0.19 / 619，需在手机覆盖安装；本次未登记后台 APP 最新版本，未开启应用内自动提醒。

## 修复范围

- 规格弹窗、购物车列表/弹层、订单详情和商品详情轮播统一处理图片失败回退。当前商品使用主图回退；订单使用成交快照内的主图，不读取当前商品替换历史价格或内容。
- H5 与 App 均将商城本地 AVIF 地址转为 `/api/media/image` 显示地址，保留原始素材；服务端依客户端 Accept 返回 AVIF 或兼容 JPEG/PNG。
- 线上只读定位确认默认 SKU 及目标历史订单 SKU 图仍为京东 `.jpg.avi` 外链。当前单规格购物车响应优先使用本地商品主图；多规格仍保留 SKU 图。未写入商品、订单或账户记录。
- 按用户确认，购物车数量按钮仅优化样式：统一高度、加大点击区域、浅色边框/输入背景及禁用状态。原有数量提交、价格与结算规则保留。

## 验证

60 组发布门禁通过：方法 44、隔离数据库 9、HTTP 1、浏览器 4、Android 2。后台/H5/App 生产构建成功，更新包 12580 个文件摘要验证通过，隔离旧安装覆盖验证保留配置、安装锁、上传、数据库标记及爬虫资料。

Android 11 / WebView 83 模拟器实际检查分类规格弹窗、购物车及失效商品、订单快照和 11 张独立商品轮播，图片均解码成功并人工核对截图。原生页面使用隔离 API；不等于实体手机或线上账户验收。H5 回归使用真实生产构建与当前公开商品素材。部署后通过公网验证 11 张商品轮播实际解码、图片 Content-Type/文件特征/304 以及前端入口摘要；线上图片与订单验证未执行支付或下单。

APK v1/v2 签名与 zipalign 通过，218 个资源逐文件匹配，48 个原生文件保持一致；已在模拟器安装并核对资源摘要，重装保留首次安装时间和测试文件标记。首次安装后的资源释放检查过早，等待资源出现后重新验证成功；没有把首次失败计为通过。

测试修正记录：订单回放最初缺少 `help_info`/`vip_true_price`，规格回放缺少 `get_attr`，已补齐接口字段；浏览器数量控件断言改为识别 UniApp 的实际组件节点。更新包校验原有“9 个服务”断言已过时，按现有 10 个服务的完整名称验证，并纳入 commerce worker 标签检查。最终全部重新验证通过。

## 部署和边界

部署目录：`/root/hengshu-mall/crmeb-mall`；回滚归档：`/root/hengshu-mall/releases/6.0.19-96b0e60af83e/rollback.tar.gz`。完成时间：`2026-09-16T02:46:36.360789+00:00`。现有配置摘要及 MySQL/Redis/采集容器身份保留，原始上传与商户私有资料未改动。

生产交易监控在更新前已有 2 条 dead 任务，更新后仍为 2 条，因此整体交易健康状态仍有告警。PHP、Nginx、队列、定时、长连接及图片等服务运行检查通过。本次未重试这些交易任务，也未清空告警；不能将本轮发布写成所有业务监控均健康。

未发布 iOS 或小程序，未提交应用商店。没有修改用户手机上的安装版本，需要用户安装新版 APK。详细门禁、截图、线上检查与发布摘要见 `dist/release-6.0.19.json` 和 `.build/release-6.0.19/`。

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| hengshu-mall-update-6.0.19.tar.gz | 101386922 | `d7627daa03e118139f349a9f1d4fc5be4e2dd5faf5cde16a725a1e08867c96be` |
| 物色-6.0.19-universal.apk | 32353695 | `97cd445d12182ba6399041cb6b1d8eae07e63efba83fbd7f4bd3a76255f79478` |
