# 物色 6.0.8：商城主题配色联动

商城风格增加 12 套预设与自定义主题色、渐变色、辅助色；首页、分类、详情、个人中心提供即时配色示例。装修读取所编辑主题的配色，客户端在页面显示、返回前台和前台每 30 秒刷新颜色，保留组件自定义色。分类三种布局提供图示和商品图片预览。商品列表价格与购物车按钮的色调开关独立生效。

使用方式和服务器命令见 [配色配置与部署说明](theme-palettes-2026-09-10.md)。

## 文件

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `dist/物色-6.0.8-universal.apk` | 32,278,977 | `4D0EF97D743BA0166490F83C19D9E6B3A393637D0C8F3AEF33F56F57241835AD` |
| `dist/hengshu-mall-update.tar.gz` | 86,192,138 | `4D30848F8CE1A5C0C0073DF814FC4C090A87F72CCC6860A4AA5FC0107319004D` |

APK 原件：`template/uni-app/unpackage/release/apk/__UNI__159D54B__20260910170744.apk`。包名 `com.hengshucredit.mall`，版本 6.0.8 / 608，沿用现有自有测试签名，v1/v2 校验通过。Android 11 模拟器由 6.0.7 覆盖安装成功，原首次安装时间保持不变；首页和分类页启动检查通过。

部署包在 2026-09-10 17:10（北京时间）生成，包含当时工作区的 PHP、后台、H5 与独立京东服务。工作区另一个装修任务正在调整分类及购物车；本次保留其改动，并在源码更新触发发布检查后重新编译和验证，未覆盖其他任务文件。后续任务产生的新改动需使用后续发布包。

## 验证

- 预设规范、HEX 校验、旧配置兼容、后台和客户端颜色映射通过；PHP 保存校验通过。
- 生产后台：12 套预设、四页示例、三种分类布局、保存重载、无效色值拦截，首页/个人中心/详情装修读取当前主题通过。
- 生产 H5：四个页面从蓝色切换为紫色，组件根颜色同步；分类显式价格色保持；商品操作按钮渐变与辅助色同步；请求失败保留旧色通过。
- 配色刷新并发合并、节流、旧预览响应隔离与网络恢复回归通过。
- 三种分类页面的配置、导航、结算栏叠放及商品操作栏默认样式/显隐/删除回归通过；购物车装修编辑器保存与删除验证通过。
- 更新包 12,413 个文件校验、现有配置/上传目录/数据保护、Compose、商城 H5 路由和静态文件、京东 API/Chromium/noVNC 以及重建后的登录数据持久化通过。

日志和截图：`.build/theme-palettes/`。Android 签名、安装和启动证据：`help/dev/.state/wuse-6.0.8-verify/`。本次没有上传服务器、上架应用市场或构建 iOS。
