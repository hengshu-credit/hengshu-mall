# 物色 6.0.16 发布记录

后端更新包已发布至 `root@111.228.52.212:/root/hengshu-mall/crmeb-mall`，相关 Docker Compose 服务已重启。Android 签名 APK 为 6.0.16 / 616，包名 `com.hengshucredit.mall`，沿用原签名。

本次包含：后台顶部手机按钮直接打开 `/pages/index/index` H5 首页；商品金额点击打开价格明细，移除独立“价格说明／查看”；统一商品店铺行和后台预览；改善商户顶部头像、店名、关注按钮及人数间距。所有上一轮可靠性修复均包含在配套更新包。

| 文件 | 大小（字节） | SHA-256 |
| --- | ---: | --- |
| hengshu-mall-update-6.0.16.tar.gz | 99802435 | `c5eba8ceb12cc8785d7bfcc9a0cd3b556a8d3d3ba62631cd06f3b368d3b90dd3` |
| 物色-6.0.16-universal.apk | 32349599 | `e4d3a7492a507f42609ad564a2ab30089b1d1588ac17ab34ae38ce0de0adbc0d` |

产物及各自校验文件位于 `dist/`。`dist/release-6.0.16.json` 记录版本、源码、各端构建、校验和部署对应关系。后端包内的 APP 资源摘要与本次签名 APK 的 `app-service.js` 相同。

源码提交 `a555c4d61e23aa613a195248041e61b4c9c4e72b`，dirty=`true`，源码摘要 `c4324ef5469c0897e43ae9ed32ddb108e1cce3b308e431f7fc4db53397d9882e`。本次未提交或清理工作区改动。配置合同与数据库迁移版本见包内 `release.json`。

验证：方法 39 组、隔离 MySQL 9 组、真实 HTTP 1 组、浏览器 3 组、Android 原生 1 组，共 53 组通过，无必需项跳过；后台、H5、App 生产构建通过。另用真实后台组件完成手机按钮浏览器点击验证，覆盖线上域名、本地网关、1617 开发端口和 HTTPS 自定义端口，保留后台标签页且不传递后台查询参数。

Android 11 / WebView 83 完成 6.0.15 → 6.0.16 覆盖安装，首次安装时间和测试数据标记保留。完成 15 个页面/弹层截图场景，包括价格明细、六页和店铺跨页预览。APK v1/v2 签名、zipalign、218 个编译资源逐文件匹配及 48 个原生模块未改变检查通过。证书 SHA-256：`7a2c34ea73302cfa070ac47003d3985ddb5bbe886431885c4a3776d8f10be7d8`。

安装后的首次原生回归未收到测试导航确认，不能计为通过。保留失败日志并重新写入已校验的测试资源后，在限制该模拟器应用只访问本地回放服务的条件下，原生回归完整通过；测试结束恢复资源并撤销临时网络规则。首轮失败不影响签名和覆盖安装结果，线上交易不在本轮验收范围。

后端包 12553 个文件 SHA-256 校验通过；环境配置、安装锁、用户上传、数据库和签名私钥等受保护状态不在包内。远程仅执行指定目录的更新包覆盖上传、相关服务停止、解包和构建启动，上传与重启退出码均为 0。完成时间：`2026-09-14T17:13:56.473127+00:00`。本地部署日志见 `.build/release-6.0.16/restart.log`。

验证范围：浏览器与 Android 使用本地隔离 API 和已保存公开配置回放，交易及权限测试在隔离环境运行。本次按用户限制未额外探测线上接口或执行独立远程巡检、清理、数据库命令。APP 交付为签名 Android 安装包，未提交应用商店，未发布 iOS 或小程序。
