# 物色 6.0.17 发布记录

后端更新包已部署至 `111.228.52.212:/root/hengshu-mall/crmeb-mall`，PHP、Nginx、队列、定时任务、长连接、图片服务和交易恢复服务已重启并通过运行状态检查。Android 签名 APK 为 6.0.17 / 617，包名 `com.hengshucredit.mall`，沿用原签名，可覆盖安装。

本次包含 Android 首页配置与开屏检查并行、首屏图片可见性补偿、移除重复懒加载、按浏览器能力原样返回 AVIF/兼容 JPEG 或透明 PNG、图片请求绕过 PHP 和转换并发合并。原始上传图片与素材库链接不变，旧 APP 图片地址继续可用。

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| hengshu-mall-update-6.0.17.tar.gz | 100099601 | `12a12307ab33060fecd9bc2c8179fc9e23d0369c8f5acacf4e7683921bf10747` |
| 物色-6.0.17-universal.apk | 32353695 | `68d0924f47d91de3ffb259442a03d67b11be0aa1da537ce2051b346c69a93b78` |

源码提交 `a555c4d61e23aa613a195248041e61b4c9c4e72b`，dirty=true，摘要 `679d1e6ded1c1d567049cc75ba8d5c999948ace02eb49d85765715e5c3ba1a7d`。本次没有提交、重置或清理工作区已有修改。后台、H5、App 均为当前源码构建；后端包中记录的 App 资源摘要与签名 APK 一致。

发布检查：方法 42 组、隔离 MySQL 9 组、HTTP 1 组、浏览器 3 组、Android 原生 1 组，共 56 组通过；另通过真实 Nginx/PHP 图片协商与 Chrome 原图解码验证。更新包 12555 个文件摘要逐一校验通过，现有配置、安装锁、上传、数据库及签名密钥不在包中。

Android 11 / WebView 83 完成 6.0.16 → 6.0.17 覆盖安装，首次安装时间和测试数据标记保留。16 个界面场景包括首屏 10/10 菜单图、六个页面、价格及规格弹层和店铺预览。APK v1/v2 签名及 zipalign 通过，218 个编译资源逐文件匹配，48 个原生模块未改变。证书 SHA-256：`7a2c34ea73302cfa070ac47003d3985ddb5bbe886431885c4a3776d8f10be7d8`。

初轮发布测试将启动测试输出中的“daily skip”误识别为跳过，改为准确描述广告绕过后重测；浏览器与原生测试首次共用了回放端口，浏览器发生端口占用，之后顺序运行通过。首次覆盖安装后的原生导航未确认，保留日志并重新写入已校验的测试资源后通过完整原生验证。失败记录不计为通过。

程序回滚备份位于 `/root/hengshu-mall/releases/6.0.17-26d533565baf/rollback.tar.gz`。图片镜像在切换前构建，更新包在服务器校验后才解压；环境配置、安装锁内容及 MySQL/Redis/采集容器身份已核对保留。程序切换完成时间：`2026-09-14T18:37:48.860754+00:00`，最终服务健康检查：`2026-09-14T18:42:52.930227+00:00`。

部署前交易监控已因缺少 `eb_commerce_task` 而异常。现有 `CommerceReliabilityInstaller` 在首次连接前调用 `getPdo()->inTransaction()`，导致启动迁移报错。本次确认目标库为 crmeb、表前缀为 eb_、无活动事务后，先用只读查询建立连接，再执行项目原有幂等迁移，补齐表结构与监控菜单；交易监控与 Docker 健康状态随后恢复正常。该安装器的冷连接源码缺陷本次未修改，操作补偿记录在 `.build/release-6.0.17/complete-schema.php` 与 `schema-completion.log`；其他缺少这些表的旧库更新时也需关注此初始化步骤。未删除或重置已有业务记录。

线上只读检查验证商城公开商品接口、导航、PNG/JPEG/AVIF 响应、304 和前端文件摘要。样本 PNG 为 476,125 字节，兼容 JPEG 为 178,011 字节，原 AVIF 为 64,868 字节；AVIF 与原上传字节摘要相同。以上不是手机真实网络测速或线上支付验收。原生交互使用本地隔离 API；未提交应用商店，未发布 iOS 或小程序。

完整来源、构建、验证、覆盖安装及部署记录位于 `dist/release-6.0.17.json`；详细日志和截图位于 `.build/release-6.0.17/`。APK 需要在手机上覆盖安装，服务器更新不会自动替换手机内置 App 资源。
