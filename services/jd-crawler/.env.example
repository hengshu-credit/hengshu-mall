# Match the running mall Docker network (docker network ls).
MALL_NETWORK=crmeb-mall_default
JD_CRAWLER_TOKEN=
# VNC authentication uses exactly eight characters; login is only exposed on host loopback.
JD_VNC_PASSWORD=
JD_LOGIN_PORT=6080
JD_CRAWLER_JOB_TIMEOUT=240
